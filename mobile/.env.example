# Android emulator: http://10.0.2.2:8000/api/v1
# Physical device: use your computer's LAN IPv4, with both on the same network.
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1
# EAS preview/production: define the deployed HTTPS URL in the EAS environment.
# Those profiles allow 90 seconds for the free API to wake up; local default is 10s.
