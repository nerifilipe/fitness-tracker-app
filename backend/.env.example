DATABASE_URL=postgresql+psycopg://fitness:fitness_local@localhost:5432/fitness
CORS_ORIGINS=["http://localhost:8081"]
# Run: python scripts/setup_local.py to generate JWT_SECRET in .env.
# JWT_SECRET must contain at least 32 random characters. Never commit it.
